name: Delete User Microservice Pipeline

on:
  push:
    paths:
      - 'delete-user-microservice/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.20'

      - name: Install Dependencies
        run: cd delete-user-microservice && go mod tidy

      - name: Run Tests
        run: cd delete-user-microservice && go test ./...

      - name: Build
        run: cd delete-user-microservice && go build -o main .

      - name: Build Docker Image
        run: |
          cd delete-user-microservice
          docker build -t delete-user-microservice .

      - name: Push to Docker Hub
        run: |
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
          docker tag delete-user-microservice ${{ secrets.DOCKER_USERNAME }}/delete-user-microservice:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/delete-user-microservice:latest
